# Copy this file to .env and fill in real values. .env is gitignored.

# Not required for weather (Open-Meteo needs no key) or claude_usage (uses
# OAuth creds in state/claude_creds.json, produced by scripts/claude_auth_setup.py).
# Reserved for future modules that need API keys.
